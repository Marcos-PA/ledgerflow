DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/ledgerflow
FRONTEND_ORIGIN=http://localhost:5173
# Generate a real one for anything beyond local dev: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=dev-secret-change-me-before-any-real-deploy
